
---

### **How to Test the Setup**
# Python Version (Specify the minimum version required)
# Ensure you have Python 3.8 or higher installed.
# You can check your Python version by running: python --version

# Core Libraries
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=0.24.0
matplotlib>=3.4.0
seaborn>=0.11.0
pillow>=8.3.0

# GUI Framework
tkinter  # Included with Python by default; no separate installation needed

# Additional Utilities
requests>=2.26.0  # Optional: For web scraping if needed in future enhancements
beautifulsoup4>=4.9.0  # Optional: For parsing HTML content if needed
selenium>=4.0.0  # Optional: For automated web crawling if needed
